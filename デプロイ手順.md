# Iyasaka Flow 公開（デプロイ）手順

本番環境にアプリを公開するための手順です。Next.js + Prisma + PostgreSQL の構成に合わせた方法を記載しています。

---

## 公開前に必要なもの

| 項目 | 説明 |
|------|------|
| **PostgreSQL** | 本番用DB（Vercel の場合は Neon / Supabase 等。Railway なら同サービス内で作成） |
| **環境変数** | `DATABASE_URL`・`AUTH_SECRET`・`AUTH_URL`（本番のURL）を必ず設定 |
| **Git リポジトリ** | GitHub 等にプッシュ済みだとデプロイが楽（Vercel / Railway は連携可能） |

---

## 方法1: Vercel で公開（推奨・Next.js に最適）

Vercel にアプリをデプロイし、データベースは **Neon** または **Supabase** の PostgreSQL を使う構成です。

### 1. データベースの準備

**Neon（無料枠あり）**

1. [Neon](https://neon.tech) でアカウント作成
2. 新規プロジェクト作成 → PostgreSQL の接続URL（Connection string）をコピー
3. 形式例: `postgresql://user:password@ep-xxx.region.aws.neon.tech/neondb?sslmode=require`

**Supabase（無料枠あり）**

1. [Supabase](https://supabase.com) でプロジェクト作成
2. Settings → Database → Connection string（URI）をコピー
3. パスワードはプロジェクト作成時に設定したもの

### 2. Vercel にデプロイ

1. [Vercel](https://vercel.com) にログイン（GitHub 連携が便利）
2. **Add New** → **Project** → このリポジトリをインポート
3. **Framework Preset**: Next.js のまま
4. **Environment Variables** で以下を設定（本番用）:

   | 名前 | 値 | 備考 |
   |------|-----|------|
   | `DATABASE_URL` | Neon/Supabase の接続URL | 必須 |
   | `AUTH_SECRET` | `openssl rand -base64 32` で生成した文字列 | 必須 |
   | `AUTH_URL` | **https://あなたのドメイン.vercel.app** | デプロイ後のURLに合わせる |

5. **Deploy** を実行

### 3. デプロイ後の初回設定

1. Vercel の **Settings** → **Domains** で表示されるURL（例: `https://crm-app-xxx.vercel.app`）を確認
2. そのURLを `AUTH_URL` に設定し直す（初回で仮のURLにしていた場合）
3. **ローカル** で Prisma を本番DBに向けてマイグレーション:

   ```bash
   DATABASE_URL="本番の接続URL" npx prisma db push
   ```

4. （任意）シードデータ投入:

   ```bash
   DATABASE_URL="本番の接続URL" npm run db:seed
   ```

これで **https://あなたのドメイン.vercel.app** でアプリが公開されます。

---

## 方法2: Railway で公開（アプリ＋DBをまとめて）

Railway はアプリと PostgreSQL を同じ場所で用意できるため、設定が少なくて済みます。

### 1. Railway の準備

1. [Railway](https://railway.app) でアカウント作成
2. **New Project** → **Deploy from GitHub repo** でこのリポジトリを選択
3. 同じプロジェクト内で **Add Service** → **Database** → **PostgreSQL** を追加
4. PostgreSQL サービスをクリック → **Variables** タブで `DATABASE_URL` をコピー

### 2. アプリの環境変数

アプリのサービス（Next.js）をクリック → **Variables** で次を追加:

| 名前 | 値 |
|------|-----|
| `DATABASE_URL` | Railway が発行した PostgreSQL の接続URL（参照で `${{Postgres.DATABASE_URL}}` などで紐付け可能） |
| `AUTH_SECRET` | `openssl rand -base64 32` で生成 |
| `AUTH_URL` | デプロイ後に表示されるURL（例: `https://xxx.up.railway.app`） |

### 3. ビルド・起動コマンド

- **Build Command**: `npm run build`
- **Start Command**: `npm run start`
- **Root Directory**: リポジトリルートのまま

### 4. デプロイ後

1. **Settings** → **Networking** → **Generate Domain** で公開URLを取得
2. そのURLを `AUTH_URL` に設定し、再デプロイ
3. ローカルで本番DBに向けて `npx prisma db push` を実行（上記 `DATABASE_URL` を使用）

---

## 方法3: Render で公開

1. [Render](https://render.com) で **New** → **Web Service**
2. リポジトリを接続し、以下を設定:
   - **Build Command**: `npm run build`
   - **Start Command**: `npm run start`
3. **New** → **PostgreSQL** でDBを作成し、**Internal Database URL** をコピー
4. Web Service の **Environment** に `DATABASE_URL`・`AUTH_SECRET`・`AUTH_URL` を設定
5. デプロイ後、発行されたURLを `AUTH_URL` に設定し直し、ローカルで `prisma db push` を実行

---

## 本番で必ず設定する環境変数

| 変数 | 必須 | 説明 |
|------|------|------|
| `DATABASE_URL` | ✅ | 本番用 PostgreSQL の接続URL |
| `AUTH_SECRET` | ✅ | 認証用シークレット（32文字以上・本番では必ず変更） |
| `AUTH_URL` | ✅ | アプリの本番URL（例: `https://your-app.vercel.app`）。NextAuth がコールバック等に使用 |

その他（任意）: `NEXT_PUBLIC_APP_URL`（フロントで参照する場合）、メール送信用の `RESEND_*` や `SMTP_*`、Google OAuth の `GOOGLE_CLIENT_ID` / `GOOGLE_CLIENT_SECRET` などは必要に応じて設定してください。

---

## デプロイ後の確認

1. ブラウザで本番URLを開く
2. **新規登録** でユーザーを作成
3. ログイン → ダッシュボード・企業・連絡先・案件などが表示されるか確認
4. **設定** → インポート/エクスポートが動作するか確認

---

## トラブルシューティング

- **`/api/auth/session` が 500（Internal Server Error）・「There was a problem with the server configuration」**  
  → **AUTH_SECRET** が Vercel の環境変数に設定されていません。  
  1. Vercel → プロジェクト → **Settings** → **Environment Variables**  
  2. **AUTH_SECRET** を追加（値は `openssl rand -base64 32` で生成した文字列）  
  3. **AUTH_URL** を追加（値は `https://iyasaka-flow.vercel.app` などデプロイ先のURL）  
  4. **Redeploy** で再デプロイ

- **「AUTH_URL が無効です」**  
  → デプロイ先の **実際のURL**（`https://` 含む）を `AUTH_URL` に設定し、再デプロイしてください。

- **「データベースに接続できません」**  
  → `DATABASE_URL` が本番DBのものか確認。Neon/Supabase の場合は `?sslmode=require` が必要なことがあります。

- **OAuth（Google ログイン）が動かない**  
  → Google Cloud Console の認証情報で「承認済みのリダイレクト URI」に `https://あなたのドメイン/api/auth/callback/google` を追加してください。
