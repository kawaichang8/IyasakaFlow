# CRM App 起動手順

## 1. 必要な環境

- **Node.js** 18 以上
- **Docker**（PostgreSQL を Docker で動かす場合）
- またはローカルに **PostgreSQL** がインストール済み

---

## 2. 依存関係のインストール

```bash
npm install
```

---

## 3. データベースの準備

### 方法A: Docker で PostgreSQL を起動（推奨）

```bash
docker-compose up -d postgres
```

- 接続情報: `postgresql://postgres:postgres@localhost:5432/crm_app`

### 方法B: 既存の PostgreSQL を使う

- データベース `crm_app` を作成し、接続URLを用意する

---

## 4. 環境変数の設定

```bash
cp .env.example .env.local
```

`.env.local` を開き、以下を設定する:

| 変数 | 説明 | 例 |
|------|------|-----|
| `DATABASE_URL` | PostgreSQL の接続URL | `postgresql://postgres:postgres@localhost:5432/crm_app` |
| `AUTH_SECRET` | 認証用シークレット（必須） | `openssl rand -base64 32` で生成 |

**AUTH_SECRET の生成例:**
```bash
openssl rand -base64 32
```
出た文字列を `AUTH_SECRET="ここに貼り付け"` のように設定する。

---

## 5. データベースのマイグレーション

```bash
npm run db:push
```

※ 初回またはスキーマ変更後に実行。テーブルが作成される。

---

## 6. サンプルデータの投入（任意）

```bash
npm run db:seed
```

テスト用のユーザー・企業・連絡先・取引などが入る。

---

## 7. 開発サーバーの起動

```bash
npm run dev
```

ブラウザで **http://localhost:3000** を開く。

---

## 8. ログインして確認する

1. **http://localhost:3000** にアクセス
2. 未ログインなら **ログイン** または **新規登録** へ
3. シードを実行した場合のテストアカウント例:
   - 管理者: `admin@crm-app.example.com` / `Admin123!`
   - 営業: `sales@crm-app.example.com` / `Sales123!`

ログイン後は以下から機能を確認できる:

- **ダッシュボード** … KPI・グラフ・最近の活動
- **企業アカウント** … 企業一覧・検索・フィルター・エクスポート
- **連絡先** … 連絡先一覧・企業紐付け・エクスポート
- **パイプライン** … 取引 Kanban・リスト・エクスポート
- **タスク** … タスク一覧・ステータス変更
- **活動履歴** … 電話・メール・ミーティング記録
- **メール** … メール送信・テンプレート
- **レポート** … 売上・パイプライン・活動レポート
- **設定** … チーム・ユーザー・権限・**データ（インポート/エクスポート）**

---

## よく使うコマンド一覧

| コマンド | 説明 |
|----------|------|
| `npm run dev` | 開発サーバー起動（http://localhost:3000） |
| `npm run build` | 本番ビルド |
| `npm run start` | 本番モードで起動 |
| `npm run db:push` | スキーマをDBに反映 |
| `npm run db:seed` | サンプルデータ投入 |
| `npm run db:studio` | Prisma Studio（DBのGUI）を開く |
| `npm run type-check` | TypeScript の型チェック |
| `npm run lint` | ESLint 実行 |

---

## トラブルシューティング

### ブラウザでアクセスできない場合

1. **開発サーバーが起動しているか確認**
   - ターミナルで `npm run dev` を実行したままにし、**止めない**
   - `Ready in 2s` や `Local: http://localhost:3000` と表示されていれば起動済み

2. **開くURLを確認**
   - **http://localhost:3000** を開く（`https` ではなく **http**）
   - ポートが 3000 であることを確認（別ポートで起動した場合はその番号）

3. **「このサイトにアクセスできません」「接続できません」と出る場合**
   - サーバーが起動していない → ターミナルで `npm run dev` を実行
   - ファイアウォールでブロックされている → ローカルアクセスの許可を確認
   - 別のPC・スマホから開いている → 下記「他デバイスからアクセスする」を参照

4. **ページは開くが真っ白・エラーになる場合**
   - `.env.local` に **AUTH_SECRET** が設定されているか確認（必須）
   - ターミナルに表示されているエラー内容を確認
   - ブラウザの開発者ツール（F12）の「コンソール」タブでエラーを確認

5. **他デバイスからアクセスする場合（同じWi‑Fi内のスマホ・別PCなど）**
   - 開発サーバーを「すべてのネットワーク」で受け付けるように起動する:
     ```bash
     npm run dev -- --hostname 0.0.0.0
     ```
   - 起動後、表示される `Network:` のURL（例: `http://192.168.x.x:3000`）を、そのデバイスのブラウザで開く

---

- **「データベースに接続できません」**  
  - Docker: `docker-compose up -d postgres` でコンテナが起動しているか確認  
  - `.env.local` の `DATABASE_URL` が正しいか確認

- **「AUTH_SECRET が設定されていません」**  
  - `.env.local` に `AUTH_SECRET` を追加（`openssl rand -base64 32` で生成）

- **ポート 3000 が使われている**  
  - `npm run dev -- -p 3001` のように別ポートで起動可能（開くURLは `http://localhost:3001`）
